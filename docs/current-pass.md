# Current Pass

Pass G is closed.

What was completed:
- Autonomous Resonant behavior now runs through the `PatternResult -> ResonantBehavior -> output` path.
- Detection-only remains the safe fallback.
- Behavior decisions and block reasons stay explainable.
- The serial interface now supports `RB log minimal` for a quieter operator view.

What to keep in mind:
- Detector parameters stayed frozen.
- Analyzer stayed measurement-only.
- The shared detection pipeline stayed simple/pass-through.

Next pass:
- To be defined.
