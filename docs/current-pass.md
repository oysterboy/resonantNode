# Pass 10 - Detection Cleanup Docs

Goal:

```text
keep active docs and source vocabulary aligned with the landed Occurrence + InspectionPlan architecture
```

Done:

```text
[LANDED] Roadmap narrowed to next cleanup only.
[LANDED] Current pass reset to a cleanup-oriented status note.
[LANDED] Active docs now point at landed architecture in myspec and keep the cleanup queue short.
```

Follow-up:

```text
[TODO] Keep future cleanup changes small enough to compile cleanly and verify with grep plus build.
[TODO] Preserve the landed architecture notes in myspec while moving only cleanup work into the roadmap.
```
