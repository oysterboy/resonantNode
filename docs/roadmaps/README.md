# Roadmaps Index

This folder contains future implementation roadmaps.

`myspec.md` remains the current architecture contract and is not regenerated by this package.

---

## Spec / Roadmap Policy

This package does not update `myspec.md`.

Later, update `myspec.md` using this docs package plus the current source code to keep the spec consistent with landed reality.

Use this rule:

```text
myspec.md:
    stable ownership / boundary rules
    landed architecture contracts
    current implementation state

roadmaps:
    planned mechanisms
    implementation order
    MVP slices
    later work

current-pass.md:
    exactly the next change

changelog.md:
    history and superseded context
```

Future-facing architecture rules may be mentioned in roadmaps as `Spec candidate` notes when they are stable enough to later constrain code.


---

## Roadmap Roles

```text
roadmap-general-node.md
    Cross-roadmap sequencing and shared node infrastructure.

roadmap-param-config.md
    Param / Config workflow, including hardcoded firmware config first and runtime config later.

roadmap-detection.md
    Future detection work only. Landed DetectionRuntime belongs in myspec.

roadmap-behavior.md
    Future behavior architecture. Full BehaviorRuntime has not landed.

roadmap-output.md
    Future SoundOutput / OutputProfile / action lifecycle work.

roadmap-vektor-later.md
    Thin later roadmap for VEKTOR exposure.
```

---

## Shared Roadmap Shape

Every roadmap should include:

```text
Architecture Goal
Current Status
Implementation Order
Next Pass / Minimum Viable First Pass
Later
Non-Goals
Spec candidates, if relevant
```

Do not build a large framework before there is a real use.

---

## MVP Guardrail

MVP does not mean throwaway.

Each minimum viable pass should be the smallest useful slice that still follows the intended architecture direction.

Avoid two extremes:

```text
too large:
    empty frameworks, generic registries, factories, unused abstractions

too small / wrong:
    hacks in Node, duplicated logic, temporary APIs, shortcuts that must be removed immediately
```

A good MVP:

```text
uses real current modules
solves a real near-term problem
keeps ownership in the right subsystem
can be extended without rewriting the same boundary
does not create compatibility sediment
```

If the quickest implementation would put logic in the wrong owner, prefer a slightly larger but correctly placed slice.

Rule:

```text
Build the smallest slice you can keep.
```


---

## Current Priority Order

The next use case is 5-node TonalPulse testing.

```text
1. Detection naming cleanup:
   Occurrence + TonalPulse.

2. 5-node status baseline:
   make status/logs show firmware identity, active profile/mode, key hardcoded params, output status if available.

3. Hardcoded param test workflow:
   tune params in code/config, upload same firmware to 5 nodes.

4. Run TonalPulse detection + behavior variation tests.

5. Only if hardcoded upload becomes too slow:
   add runtime Param LIST/GET, then SET + SAVE/LOAD or fleet apply/verify.

6. After tests:
   decide DetectionProfile cleanup, PulseSequence / pulsed chirp, and distinct BehaviorPrograms.
```

---

## Key Constraint

Runtime `PARAM SET` without persistence or fleet apply/verify is not enough for 5-node testing.

Use one of these workflows:

```text
Route A:
    hardcoded config + firmware upload

Route B:
    PARAM SET + SAVE/LOAD + VERIFY

Route C:
    fleet/OTA param apply + VERIFY
```

Do not stop at unsaved runtime params as the main test workflow.
